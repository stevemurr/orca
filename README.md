# orca

A terminal client for agent harnesses.

`orca` keeps the conversation primary, makes durable run state legible, and talks to any
backend that implements a small documented interface. The full contract lives in
[`docs/backend-contract.md`](docs/backend-contract.md).
